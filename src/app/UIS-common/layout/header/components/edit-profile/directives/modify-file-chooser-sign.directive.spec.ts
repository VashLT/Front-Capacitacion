import { ModifyFileChooserSignDirective } from "./modify-file-chooser-sign.directive";

describe("ModifyFileChooserSignDirective", () => {
  it("should create an instance", () => {
    const directive = new ModifyFileChooserSignDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
