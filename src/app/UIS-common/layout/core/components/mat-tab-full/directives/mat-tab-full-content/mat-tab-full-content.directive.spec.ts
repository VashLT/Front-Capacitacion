import { MatTabFullContentDirective } from "./mat-tab-full-content.directive";

describe("MatTabFullContentDirective", () => {
  it("should create an instance", () => {
    const directive = new MatTabFullContentDirective(null);
    expect(directive).toBeTruthy();
  });
});
