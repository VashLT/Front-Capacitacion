import { DatesChangePipe } from './dates-change.pipe';

describe('DatesChangePipe', () => {
  it('create an instance', () => {
    const pipe = new DatesChangePipe();
    expect(pipe).toBeTruthy();
  });
});
