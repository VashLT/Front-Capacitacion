/**
 * Servicios proveídos a nivel root dentro del módulo actual (capacitación),
 * NO se incluyen aquellos dentro del submódulo layout pero SI de otros submódulos si aplican.
 */
export const MODULE_PROVIDERS = [];
